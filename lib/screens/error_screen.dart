import 'package:flutter/material.dart';

class ErrorScreen extends StatelessWidget {
  // Shows Error Screen, Once a error is thrown
  @override
  Widget build(BuildContext context) {
    //TODO: Build an Error Screen
    return Container();
  }
}
